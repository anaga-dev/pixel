/**
 * Guarda un archivo en formato .FPG
 *
 * @param {PixelDocument} document
 * @returns {Blob}
 */
export async function save(document) {

}

/**
 * Carga un archivo en formato .FPG
 *
 * @param {Blob} blob
 * @returns {Promise<PixelDocument>}
 */
export async function load(blob) {

}
